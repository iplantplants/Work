import java.awt.*;import java.awt.event.*;import javax.swing.*;import static javax.swing.BorderFactory.*;class MakeWorld extends JFrame {   private JButton jbtnReload;   private JPanel jpnlButtons;   RobotWorld w;   public static void main(String[] args) {      new MakeWorld().setVisible(true);      }   public MakeWorld() {         w = new RobotWorld();      w.init();      w.start();         Container cp = getContentPane();      cp.add(w, BorderLayout.CENTER);      // cp.add(makeButtonPanel(),BorderLayout.SOUTH);       setLocation(800,120);      pack();         addWindowListener(                new WindowAdapter() {                   @Override                   public void windowClosing(WindowEvent evt) {                      System.exit(0);                   }                });                }   // private JPanel makeButtonPanel() {      // jpnlButtons = new JPanel();      // jpnlButtons.setPreferredSize(new Dimension(100, 50));      // jbtnReload = new JButton("Reload");      // jbtnReload.setMaximumSize(new Dimension(60, 20));      // jpnlButtons.setLayout(new BorderLayout());      // jpnlButtons.setBorder(createEtchedBorder());   //          // jbtnReload.addActionListener(         // (ActionEvent evt) -> {            // jbtnReloadActionPerformed(evt);         // });      // jpnlButtons.add(jbtnReload, BorderLayout.CENTER);      // return jpnlButtons;   // }//    // private void jbtnReloadActionPerformed(ActionEvent evt) {           //    // }}